#include <stdio.h>

int main(int argc, char const *argv[])
{
    // man ascii
    // 字符 ascii 码
    char a = 'a';
    printf("char a: %c, a ascii: %d\n", a, a);

    char a_num = 97;
    printf("char a: %c, a ascii: %d\n", a_num, a_num);

    return 0;
}
